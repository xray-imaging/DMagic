package:
  name: dmagic
  version: '0.4'

source:
  path: src
  
build:
  number: 0

requirements:
  build:
    - python
    - setuptools
 
  run:
    - python

#test:
  # Python imports
#  imports:
#    - globus
#    - scheduling

about:
  home: http://dataexchange.readthedocs.org
  license: BSD
  summary: 'Reader/Writer for Data Exchange files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml

